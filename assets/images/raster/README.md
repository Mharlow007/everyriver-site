Place a background photo here named `hero.jpg`.

Recommended:
- Dimensions: at least 1920×1080 (or larger for retina)
- Format: JPG or WebP (if JPG, use progressive)
- Size: ideally < 500 KB after compression

The preview home header uses `/assets/images/raster/hero.jpg` with a dark overlay.
